@page "/app/runs"
@layout AppLayout
@attribute [Authorize]

<PageTitle>Runs · Crawldad</PageTitle>

@*
    Empty-state placeholder. The runs table + filters are wired in a later task (#67) over Crawldad.Client.
*@
<div class="page-header">
    <h1 class="page-title">Runs</h1>
</div>

<div class="card mt-3">
    <div class="empty">
        <div class="empty-icon">
            <svg xmlns="http://www.w3.org/2000/svg" class="icon" width="48" height="48" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none" /><path d="M9 6l11 0" /><path d="M9 12l11 0" /><path d="M9 18l11 0" /><path d="M5 6l0 .01" /><path d="M5 12l0 .01" /><path d="M5 18l0 .01" /></svg>
        </div>
        <p class="empty-title">No runs yet</p>
        <p class="empty-subtitle text-secondary">
            Every scrape or search run streams in here as it moves through queued, running, and terminal states.
        </p>
    </div>
</div>
